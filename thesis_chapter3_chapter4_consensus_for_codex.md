# 硕士论文第三章与第四章方案共识

更新日期：2026-07-29

本文件用于约束后续代码实现、实验设计和论文写作。第三章和第四章面向同一个任务：遥感图像旋转目标检测，但不再设计成严格的模型递进关系，也不再把第四章定位为解决第三章计算量上升的问题。两章应作为并列互补的技术路线来组织。

## 1. 总体关系

第三章和第四章分别关注不同网络阶段、不同问题分析和不同解决机制：

- 第三章研究 Neck 和 Head/预测端如何更好地传递、融合和预测小目标细节。
- 第四章研究 Backbone 如何从特征提取源头增强复杂遥感场景的自适应感受野和上下文表达。

跨章节结果可以保留，用于说明两条路线的性能特点和适用场景，但不能写成同容量、同基础模型下的公平绝对排名。

推荐总述：

> 第三章方法在较低模型复杂度下重点增强旋转小目标检测能力；第四章方法采用容量更高的遥感专用骨干，侧重复杂场景下的整体特征提取和检测精度增强。两种方法分别对应轻量小目标增强和精度优先检测需求。

## 2. 第三章定位

第三章以 YOLO11n-OBB 为基础模型，创新点由 FSPB 和 LPCF 两个模块组成，主要从 Neck 和 Head/预测端改善：

- 小目标细粒度信息在下采样和特征融合过程中的丢失；
- 小目标特征传递与预测能力不足；
- 多尺度上下文融合不足。

第三章主要特点：

- 基础模型较轻；
- 参数量增加很少；
- Small mAP 提升较明显；
- 更偏向轻量模型上的小目标检测增强。

第三章内部消融必须证明：

| Variant | A: FSPB | B: LPCF |
|---|---:|---:|
| YOLO11 baseline |  |  |
| YOLO11 + A | ✓ |  |
| YOLO11 + B |  | ✓ |
| YOLO11 + A + B | ✓ | ✓ |

## 3. 第三章主结果

第三章最终模型 A+B 的 DIOR-R official test 结果：

| Model | Params (M) | GFLOPs | All mAP50 | All mAP50:95 | Small mAP50 | Small mAP50:95 |
|---|---:|---:|---:|---:|---:|---:|
| YOLO11n-OBB baseline | 2.658 | 6.6 | 71.11 | 54.31 | 27.32 | 17.96 |
| YOLO11n-OBB + A + B | 2.740 | 10.7 | 72.25 | 54.55 | 29.20 | 20.42 |

第三章三 seed 稳定性结果以 `paper/ippr2026/main.pdf`、`paper/ippr2026/main.tex` 和根目录 `README.md` 中的定稿表为准。

## 4. 第四章定位

第四章采用：

```text
LSKNet-T Backbone
+ necessary channel adapters
+ original YOLO11 Neck
+ original YOLO11 OBB Head
```

第四章不继承第三章的 FSPB 和 LPCF。LSKNet-T 的引入只是第四章基础架构选择，不作为本文创新。第四章真正的第二个创新点仍由后续 C、D 两个模块组成。

第四章定位为：

> 基于遥感专用自适应感受野骨干的精度增强型旋转目标检测方法。

主要研究：

- 不同尺度和不同形状遥感目标所需上下文范围存在差异；
- 常规卷积骨干的固定局部感受野难以充分建模复杂遥感场景；
- 需要增强自适应感受野、长程空间关系和复杂背景特征表达；
- C、D 应针对 LSKNet-T baseline 的具体不足进一步提升检测精度。

第四章不再定位为轻量化，也不要求 Params 或 GFLOPs 下降。Params、GFLOPs、FPS、延迟和显存必须如实报告，但它们是复杂度分析指标，不是第四章的核心优化目标。

## 5. 当前 LSKNet-T Baseline 解读

当前 DIOR-R official test 结果：

| Model | Params (M) | GFLOPs | All mAP50 | All mAP50:95 | Small mAP50 | Small mAP50:95 |
|---|---:|---:|---:|---:|---:|---:|
| YOLO11n-OBB + A + B | 2.740 | 10.7 | 72.25 | 54.55 | 29.20 | 20.42 |
| LSKNet-T baseline | 5.728 | 18.7 | 73.72 | 56.88 | 27.69 | 18.15 |

相对于第三章 A+B：

- 参数量约为 2.09 倍；
- GFLOPs 约为 1.75 倍；
- All mAP50:95 高 2.33；
- Small mAP50:95 低 2.27。

因此不能直接写：

> LSKNet-T Backbone 优于第三章 A+B。

也不能写：

> LSKNet-T Backbone 优于 YOLO11 原生 Backbone。

因为两者模型容量和计算量差异明显，现有结果不能排除“模型更大所以全尺度精度更高”的影响。

当前结果只能说明：

1. LSKNet-T 的 DOTA 权重和 YOLO11 Neck/OBB Head 的混合结构接入成功；
2. 新 baseline 可以稳定训练和收敛；
3. LSKNet-T baseline 具有较强的全尺度检测潜力；
4. 其小目标检测能力仍明显低于第三章 A+B；
5. 该 baseline 可以继续作为第四章的候选基础模型。

不要把“LSKNet-T baseline 比第三章 A+B 的 All mAP 更高”作为第四章成立的核心证据。

## 6. 第四章内部消融

第四章不必与第三章直接比输赢。第四章成立的核心条件是：

- C 单独带来稳定提升；
- D 单独带来稳定提升；
- C+D 优于 LSKNet-T baseline；
- 主要提升在两个数据集和多随机种子下具有稳定性；
- C、D 的机制和解决的问题与第三章 A、B 不重复。

第四章内部消融形式：

| Variant | C | D |
|---|---:|---:|
| LSKNet-T baseline |  |  |
| LSKNet-T + C | ✓ |  |
| LSKNet-T + D |  | ✓ |
| LSKNet-T + C + D | ✓ | ✓ |

不要求：

```text
LSKNet-T + C + D 必须全面优于 YOLO11 + A + B
```

如果第四章最终模型表现为总体 mAP 明显高于第三章、小目标 mAP 与第三章接近或略低、参数量和 GFLOPs 明显更大，也可以成立。前提是 C、D 相对于 LSKNet-T baseline 的提升明确，并且第四章在其声称解决的问题上有充分实验支撑。

## 7. C、D 设计边界

第四章后续 C、D 可以重点关注以下一种或多种问题：

- LSKNet-T baseline 对小目标细粒度信息利用不足；
- 旋转目标的方向和几何特征建模不足；
- 浅层细节与深层大感受野语义之间交互不足；
- 复杂背景下类别判别或旋转框定位不足。

C、D 不应：

- 简单重复 LSKNet 已有的大核上下文机制；
- 直接复制第三章的 FSPB；
- 直接复制第三章的 LPCF；
- 再增加一个完全相同的高分辨率预测分支；
- 为了强行与第三章比较而设计模块。

## 8. 是否做容量匹配实验

构造约 5.7M 参数、18.7 GFLOPs 的 YOLO11 容量匹配模型有研究价值，但不是当前硕士论文的必做核心实验。

只有在论文准备声明：

> LSKNet-T Backbone 本身优于 YOLO11 原生 Backbone。

时，才需要通过容量匹配实验排除模型规模影响。当前论文应避免上述声明，将 LSKNet-T 明确作为非创新的基础架构选择，因此容量匹配实验可以列为可选补充，而不是阻塞第四章后续工作的前置条件。

## 9. 章节衔接参考表述

第三章结尾可按以下逻辑组织：

> 第三章主要从特征融合与预测阶段入手，通过细尺度特征保持和上下文融合改善旋转小目标检测性能。然而，遥感场景中的目标在尺度、形态和空间分布方面具有显著差异，仅在 Neck 和检测端进行特征补偿，仍难以充分研究骨干网络特征提取机制对复杂场景检测性能的影响。因此，第四章进一步从特征提取阶段出发，研究基于遥感专用自适应感受野骨干的精度增强方法。

第四章开头可按以下逻辑组织：

> 本章采用 LSKNet-T 作为基础骨干，利用其根据目标特征动态选择不同范围空间上下文的能力，增强复杂遥感场景下的特征表达。在此基础上，针对 LSKNet-T 旋转检测基线在某些尺度、方向或细粒度特征建模方面的不足，提出 C 模块和 D 模块，以进一步提升旋转目标检测精度。LSKNet-T 的引入属于基础架构选择，不作为本文创新，本文创新主要体现在 C、D 的结构设计及其协同机制。

## 10. 后续执行顺序

正式实验目标接受两个数据集、四组消融、三随机种子，即 `2 × 4 × 3 = 24` 次完整训练，但采用分阶段流程：

1. 已完成：DIOR-R 单种子 LSKNet-T baseline 验证；
2. 下一步：基于 baseline 暴露的问题设计 C、D；
3. 先进行 C、D 单种子筛选；
4. 设计成立后，运行两数据集三种子的正式消融实验；
5. 完整报告 Params、GFLOPs、FPS、延迟、显存和 mAP。

第四章硬指标以 mAP 为主，尤其是 C、D 相对于 LSKNet-T baseline 的提升。复杂度指标用于分析部署代价和适用场景，不作为必须下降的约束。
